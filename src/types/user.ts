export type UserType = {
  id: string;
  name: string;
  email: string;
  photoURL?: string;
  group: string;
  numeral?: string;
  city?: string;
  state?: string;
  isAdmin?: boolean;
  totalPoints?: number;
};
